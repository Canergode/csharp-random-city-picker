# Random City Picker

This project randomly picks a city from a string array by generating a random index using the Random class.

## What I Learned
- How to use Random to pick a random array element
- How to generate a random index within array bounds
- How to combine Random with arrays for random selection
- How Random.Next(n) generates values from 0 to n-1
